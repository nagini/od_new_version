{% extends "AdminBundle::admin_layout.html.twig" %}

{% block menu %}
	{{ knp_menu_render('AdminBundle:Builder:adminMenu') }}
{% endblock %}

{% block rastro %}{{ wo_render_breadcrumbs() }}{% endblock %}

{% block titulo %}Nuevo contrato{% endblock %}


{% block cuerpo %}

	<table class="table table-hover">
		<thead>
			<tr>
				<th colspan='2'>Cliente</th>
			</tr>
		</thead>
        <tbody>
            <tr>
                <th><strong>Nit</strong></th>
                <td>{{ cliente.nit }}</td>
            </tr>
            <tr>
                <th><strong>Nombre</strong></th>
                <td>{{ cliente.nombre }}</td>
            </tr>
            <tr>
                <th><strong>Código de la eps</strong></th>
                <td>{{ cliente.codEps }}</td>
            </tr>
        </tbody>
    </table>

<form action="{{ path('contrato_save', {'id':id}) }}" method="post" {{ form_enctype(form) }}>
    <fieldset>
    	<legend>Datos del contrato</legend>
    	{{ form_widget(form) }}
    </fieldset>
    
    <button class="btn btn-success" type="submit">Crear</button>
    <a href="{{ path('cliente_show', {'id':id}) }}" class="btn">Regresar al listado</a>
    
</form>
{% endblock %}
