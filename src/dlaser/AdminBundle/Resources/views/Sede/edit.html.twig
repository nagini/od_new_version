{% extends "AdminBundle::admin_layout.html.twig" %}

{% block menu %}
	{{ knp_menu_render('AdminBundle:Builder:adminMenu') }}
{% endblock %}

{% block rastro %}{{ wo_render_breadcrumbs() }}{% endblock %}

{% block titulo %}Nueva empresa{% endblock %}


{% block cuerpo %}
<form action="{{ path('sede_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <fieldset>
    	<legend>Datos de la sede</legend>
    	{{ form_widget(edit_form) }}
    </fieldset>
    
    <button class="btn btn-success" type="submit">Modificar</button>
    <a href="{{ path('sede_show', {'id': entity.id}) }}" class="btn">Detalle de la sede</a>
    
</form>

<form action="{{ path('sede_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <button class="btn btn-danger" type="submit" onclick="return confirm('¿Realmente desea eliminar la sede?');">Eliminar</button>
</form>
{% endblock %}
